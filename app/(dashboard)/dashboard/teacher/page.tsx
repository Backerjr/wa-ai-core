import { TeacherDashboard } from "@/components/views/teacher-dashboard";

/**
 * The TeacherPage component renders the TeacherDashboard component.
 * This component is the top-level component for the teacher dashboard.
 *
 * @returns A JSX element representing the teacher dashboard page.
 */
export default function TeacherPage(): JSX.Element {
  /**
   * Renders the TeacherDashboard component.
   */
  return <TeacherDashboard />;
}
